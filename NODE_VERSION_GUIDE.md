# 📦 Node.js Version Guide for Health Monitor App\n\n---\n\n## Your Current Setup\n\n### Check Your Node Version\n\n**Open PowerShell and run:**\n```powershell\nnode --version\n```\n\n**You should see something like:**\n```\nv14.21.3\nv16.20.0\nv18.17.0\nv20.9.0\n```\n\n---\n\n## What Version Does Your App Need?\n\n### Current Requirement (package.json)\n\n**Looking at your code:**\n```json\n\"main\": \"server/app.js\",\n\"engines\": {\n  \"node\": \">=14.0.0\"\n}\n```\n\n**This means:**\n- ✅ Minimum: Node 14.0.0\n- ✅ Recommended: Node 16+ or 18+\n- ✅ Latest: Node 20+\n\n---\n\n## Node.js Version Comparison\n\n### Versions & Features\n\n| Version | Status | Release Date | Support Ends | Your App |\n|---------|--------|--------------|--------------|----------|\n| **14.x** | Old | April 2020 | April 2023 | ✅ Works |\n| **16.x** | Old | April 2021 | September 2023 | ✅ Works |\n| **18.x** | LTS | April 2022 | April 2025 | ✅ Recommended |\n| **20.x** | Current | October 2023 | October 2026 | ✅ Best |\n\n---\n\n## Check Your Version\n\n### Step 1: Open PowerShell\n\n```powershell\nnode --version\n```\n\n**Result:**\n- If you see `v14.x` → Older version (still works)\n- If you see `v16.x` → Good version (still works)\n- If you see `v18.x` → Great version (recommended)\n- If you see `v20.x` → Latest version (best)\n\n---\n\n## What Version Should You Use?\n\n### For Your Health Monitor App\n\n**Minimum Required:**\n```\nNode 14.x (works, but old)\n```\n\n**Recommended:**\n```\nNode 18.x (LTS - Long Term Support)\nNode 20.x (Latest)\n```\n\n**Why Node 18+?**\n- ✅ Better performance\n- ✅ Security updates\n- ✅ Better async/await\n- ✅ More packages support it\n- ✅ Better npm compatibility\n\n---\n\n## Install/Update Node.js\n\n### Option 1: Using Installer (Easiest)\n\n1. Go to: https://nodejs.org/\n2. Download **LTS version** (18.x or 20.x)\n3. Run installer\n4. Follow steps\n5. Restart computer\n6. Verify: `node --version`\n\n**Time**: 5 minutes\n\n---\n\n### Option 2: Using nvm (Windows)\n\n**Install nvm-windows:**\n\n1. Download from: https://github.com/coreybutler/nvm-windows/releases\n2. Download: `nvm-setup.exe`\n3. Run installer\n4. Restart PowerShell\n\n**Install Node:**\n\n```powershell\n# List available versions\nnvm list available\n\n# Install latest LTS\nnvm install lts\n\n# Use specific version\nnvm use 20.9.0\n\n# Check version\nnode --version\n```\n\n**Time**: 10 minutes\n\n---\n\n### Option 3: Using Chocolatey\n\n```powershell\n# Install Chocolatey first (if not installed)\nSet-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))\n\n# Then install Node\nchoco install nodejs\n\n# Update Node\nchoco upgrade nodejs\n```\n\n**Time**: 10 minutes\n\n---\n\n## Verify Installation\n\n### Check Node\n\n```powershell\nnode --version\n```\n\n**Should show:** `v18.x.x` or higher\n\n### Check npm\n\n```powershell\nnpm --version\n```\n\n**Should show:** `8.x.x` or higher\n\n### Test\n\n```powershell\n# Create test file\necho console.log('Node works!') > test.js\n\n# Run it\nnode test.js\n\n# Clean up\nrm test.js\n```\n\n---\n\n## Dependencies Compatibility\n\n### Your Current Dependencies\n\n```json\n{\n  \"express\": \"^4.18.2\",           ← Works with Node 14+\n  \"body-parser\": \"^1.20.2\",       ← Works with Node 14+\n  \"cors\": \"^2.8.5\",               ← Works with Node 14+\n  \"uuid\": \"^9.0.0\",               ← Works with Node 14+\n  \"googleapis\": \"^118.0.0\",        ← Works with Node 14+\n  \"google-auth-library\": \"^8.8.0\",← Works with Node 14+\n  \"express-session\": \"^1.17.3\",   ← Works with Node 14+\n  \"dotenv\": \"^16.0.3\"             ← Works with Node 14+\n}\n```\n\n**All packages work with:**\n- ✅ Node 14.x\n- ✅ Node 16.x\n- ✅ Node 18.x (Recommended)\n- ✅ Node 20.x (Latest)\n\n---\n\n## Azure Deployment Requirements\n\n### For Azure App Service\n\n**You need to specify:**\n\n```\nRuntime Stack: Node.js 18.x\nOR\nRuntime Stack: Node.js 20.x\n```\n\n**How to set in Azure:**\n\n```powershell\n# Using Azure CLI\naz webapp config set --resource-group health-monitor --name health-monitor-app --linux-fx-version \"NODE|18.0\"\n\n# Or in Azure Portal:\n# Settings → Configuration → General Settings → Runtime Stack → Node.js 18\n```\n\n---\n\n## Update npm Packages\n\n### Check Outdated Packages\n\n```powershell\ncd c:\\Users\\vinutha.gowde\\workspace\\healthMoniter\nnpm outdated\n```\n\n**You should see something like:**\n```\nPackage              Current  Wanted  Latest\nexpres               4.18.2   4.18.2  4.18.3\n```\n\n### Update All Packages\n\n```powershell\n# Safe update (respects ^versions)\nnpm update\n\n# Aggressive update (may break things)\nnpm upgrade\n\n# Update specific package\nnpm install express@latest\n```\n\n### Verify After Update\n\n```powershell\ncd c:\\Users\\vinutha.gowde\\workspace\\healthMoniter\nnpm start\n```\n\n---\n\n## Node Version in package.json\n\n### Update Your Requirements\n\n**Current:**\n```json\n{\n  \"engines\": {\n    \"node\": \">=14.0.0\"\n  }\n}\n```\n\n**Better:**\n```json\n{\n  \"engines\": {\n    \"node\": \">=18.0.0\",\n    \"npm\": \">=9.0.0\"\n  }\n}\n```\n\n**How to update:**\n\n```powershell\n# Open package.json in editor\n# Find \"engines\" section\n# Change to:\n\"engines\": {\n  \"node\": \">=18.0.0\",\n  \"npm\": \">=9.0.0\"\n}\n```\n\n---\n\n## Troubleshooting\n\n### Issue: \"npm: The term 'npm' is not recognized\"\n\n**Solution:**\n1. Uninstall Node completely\n2. Restart computer\n3. Reinstall Node from nodejs.org\n4. Restart again\n5. Check: `npm --version`\n\n---\n\n### Issue: \"node: command not found\"\n\n**Solution:**\n```powershell\n# Check Node install location\nwhere node\n\n# If nothing shows, reinstall:\n# 1. Go to nodejs.org\n# 2. Download installer\n# 3. Run with \"Admin\"\n# 4. Check \"Add to PATH\"\n# 5. Restart PowerShell\n```\n\n---\n\n### Issue: \"Module not found\" after update\n\n**Solution:**\n```powershell\n# Clear cache\nnpm cache clean --force\n\n# Remove node_modules\nrm -r node_modules\n\n# Reinstall\nnpm install\n```\n\n---\n\n## Best Practices\n\n### Use `.nvmrc` File\n\n**Create in project root:**\n```\n18.17.0\n```\n\n**Then run:**\n```powershell\nnvm use\n```\n\n**This ensures everyone uses same version**\n\n---\n\n### Lock Dependencies\n\n**Your app should have:**\n```\npackage-lock.json (already exists)\n```\n\n**This locks exact versions used**\n\n**Always commit to git:**\n```powershell\ngit add package-lock.json\ngit commit -m \"Lock dependencies\"\n```\n\n---\n\n## Recommended Setup\n\n### Ideal Configuration\n\n```\nLocal Development:\n  Node: 18.x or 20.x (latest LTS)\n  npm: 9.x or 10.x\n  \nAzure Production:\n  Runtime: Node.js 18.x or 20.x\n  \npackage.json:\n  \"engines\": {\n    \"node\": \">=18.0.0\",\n    \"npm\": \">=9.0.0\"\n  }\n```\n\n---\n\n## Quick Checklist\n\n- [ ] Check current version: `node --version`\n- [ ] Check npm version: `npm --version`\n- [ ] Update if needed (Node 14+ required, 18+ recommended)\n- [ ] Update package.json engines\n- [ ] Run `npm install` to verify\n- [ ] Run `npm start` to test\n- [ ] All working? ✅\n\n---\n\n## Summary\n\n### For Your Health Monitor App\n\n**Minimum**: Node 14.x ✅\n**Recommended**: Node 18.x or 20.x ⭐\n**Azure**: Node 18.x or 20.x ⭐\n\n### Current Status\n\n**Your app works with:**\n- ✅ Node 14.x (if you have it)\n- ✅ Node 16.x (if you have it)\n- ✅ Node 18.x (best for now)\n- ✅ Node 20.x (latest, also great)\n\n### Action\n\n1. **Check version:** `node --version`\n2. **If < 18**: Update from nodejs.org\n3. **If ≥ 18**: You're good!\n4. **Test:** `npm start` should work\n\n---\n\n**What version do you have?** 🤔\n\nRun: `node --version` and tell me the result!\n